{
  "name": "@clarity/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "npm run build -w @clarity/domain && tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "npm run build -w @clarity/domain && tsc -p tsconfig.json --noEmit",
    "test": "npm run build -w @clarity/domain && vitest run",
    "typecheck": "npm run build -w @clarity/domain && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clarity/domain": "0.1.0",
    "@fastify/cors": "^10.0.2",
    "@fastify/jwt": "^9.1.0",
    "@prisma/client": "^6.5.0",
    "fastify": "^5.2.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
